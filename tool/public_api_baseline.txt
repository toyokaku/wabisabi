# Known public-surface violations. tool/check_public_api.dart fails on anything
# NOT listed here, so this file can only shrink: fix the violation, delete the
# line. It also fails on entries that no longer apply, so it cannot rot.
#
# PREFIX  the name is not Wab / WAB_ / kWab / wab prefixed, so it lands bare in
#         the consumer's namespace. Renaming these is a breaking change on the
#         public API and wants a version bump, which is why they are parked
#         rather than quietly fixed.
#
# EXAMPLE (example coverage) is clear — every exported type is drawn in
# example/lib. Keep it that way; a new export with no specimen fails CI.

PREFIX DeckleBorder lib/materials/deckle_border.dart
PREFIX DotGrid lib/components/dot_grid.dart
PREFIX InkDot lib/components/dot_grid.dart
PREFIX InkDotStyle lib/components/dot_grid.dart
PREFIX TexturePainter lib/components/scaffold.dart
PREFIX isIos lib/components/wab_utils.dart
